services:
  angular-app:
    build: .
    container_name: adoption-animal-app
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    networks:
      - controlimage_network

networks:
  controlimage_network:
    external: true
